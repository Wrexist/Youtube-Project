---
description: Start or resume a build phase from PLAN.md
argument-hint: <phase number, e.g. 3>
---

Work on **Phase $1** from [PLAN.md](PLAN.md).

1. Read PLAN.md and restate the phase's goal, steps, and exit criterion in two or three lines.
2. Check what already exists in the repo — do not rebuild finished work. Report anything already done.
3. Load the skills relevant to this phase (`studio-ui` for UI work, `render-pipeline` for engine work, `youtube-seo`, `youtube-publishing`, `script-architecture`, `thumbnail-design`).
4. Create tasks for the remaining steps, then work through them.
5. Stop at the phase's exit criterion and **demonstrate it** — run it, show output, don't just claim it.

If a step in the plan turns out to be wrong once you see the code, say so and propose the correction before implementing something different.
